# AI Clustering Presentation

## Setup 

* Create virtual env

```bash
python3 -m venv .venv
source .venv/bin/activate
```

* Install dependencies

```bash
pip3 install -r requirements.txt
```

* Register kernal

```bash
python3 -m ipykernel install --user --name=clustering-presentation --display-name "Clustering presentation"
```